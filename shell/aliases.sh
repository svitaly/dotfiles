alias la="ls -la"
alias c="clear"
alias g="git"
alias gitsquash='git reset --soft HEAD~$(git rev-list --count HEAD ^master)'

source ~/.env
# remember to start ssh chanels for socks and tmux ssh separately
# ALSO turn on VPN to USA = this HELPS immensely

